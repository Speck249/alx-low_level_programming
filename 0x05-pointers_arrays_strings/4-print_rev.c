#include "main.h"
/**
*print_rev - prints a string, in reverse
*@s: parameter
*Return: no return value
*/

void print_rev(char *s)
{
int n, i;

while (str[n] != '\0')
{
n++
}

for (i = n; i >= 0; i--)
{
_putchar(str[i]);
}

if (str[i] == '\0')
_putchar('\n');
}
