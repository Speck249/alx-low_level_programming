#include "main.h"
/**
*_abs - function computes the absolute value of an integer
*
*Return: Always 0 (Success)
*/

int _abs(int)
{
int n, x;
n = abs(x);
if (x == 0)
{
return (0);
}
else
{
return (n);
}
_putchar('\n');
}
